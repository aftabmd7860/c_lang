#include<stdio.h>
int main()
{
	int n,count,sum=0;
	printf("enter the number\n");
	scanf("%d",&n);	
		while(count<=n)	
		if (count%2!=0)
		sum=sum+count;
		count++;	
			

	printf("sum of odd num is %d\n",sum=sum+count);
	return 0;
}
