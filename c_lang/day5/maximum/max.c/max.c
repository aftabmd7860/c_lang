// maximum of 2 number
#include<stdio.h>
int main()
    { 
      int a, b, max;
      printf("enter the value\n");
      scanf("%d%d",&a,&b);
      // compare the num1 and num2
    // if 
    //  (a>b);
     //{ printf("%d, a is max);
    // }
    // else

     max = (a>b) ? a : b;

     { printf("max between %d and % is %d\n", a ,b ,max);
		     }
return 0; }
